{
  "name": "decora",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "prepack": "bun test",
    "release": "bunx standard-version && git push --follow-tags origin main && npm publish"
  },
  "version": "0.0.3"
}
